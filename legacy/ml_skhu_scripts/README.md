# Legacy ML_SKHU Scripts

These scripts belong to the older `ML_SKHU` training experiments. They are kept
for reference, but they are not the active online-agent pipeline.

Use the root-level `run_ml_ab_*.sh` scripts and the `ML_AB/` package for current
model training, evaluation, live MCTS, and online deployment work.
